<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ergomanager"
    android:versionCode="1"
    android:versionName="ErgoManager 1.0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_be_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_be_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ErgoManager">
        <activity android:name=".About"></activity>
        <activity android:name=".RGPDConsent" />
        <activity android:name=".LoginPin" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlanList" />
        <activity android:name=".NordicList" />
        <activity android:name=".NordicCRUD" />
        <activity android:name=".UserCRUD" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize" />
    </application>

</manifest>